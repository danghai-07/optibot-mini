# Screenshot placeholder

Run `python scripts/test_chat.py` after a successful sync, then capture the terminal showing:

- Question: *How do I add a YouTube video?*
- OptiBot answer with `Article URL:` citations

Save the image as `screenshot-playground.png` in this folder.
